{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "garfield-router.fullname" . }}
  labels:
    {{- include "garfield-router.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "garfield-router.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      interval: {{ .Values.serviceMonitor.interval }}
      path: {{ .Values.serviceMonitor.path }}
{{- end }}
